# Study trial versions

`trials.json` is the active Study geometry. `trials_pilot_v1.json` is the
frozen `pilot_v1` snapshot used to reproduce the pilot. Any geometry change
after `pilot_v1` must first create a new named snapshot (for example
`trials_pilot_v2.json`) before replacing the active definition.
